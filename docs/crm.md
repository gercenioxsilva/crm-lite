# CRM (básico) - Domínio de Leads

Entidades iniciais:
- leads(id, name, email, source, created_at)
- (futuro) pipelines, stages, activities, notes, tags, users

Migrações iniciais em `services/leads/db/migrations`.
